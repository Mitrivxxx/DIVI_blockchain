import React from 'react';

const Dashboard: React.FC = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Podsumowanie dokumentów, ostatnie weryfikacje, statystyki.</p>
    </div>
  );
};

export default Dashboard;
